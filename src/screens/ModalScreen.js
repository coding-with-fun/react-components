import React from "react";
import Modal from "../components/Modal";

const ModalScreen = () => {
    return (
        <div>
            <Modal />
        </div>
    );
};

export default ModalScreen;
