import React from "react";
import Loader from "../components/Loader";

const LoaderScreen = () => {
    return (
        <div>
            <h1>I am Loader screen</h1>
            <Loader />
        </div>
    );
};

export default LoaderScreen;
